SELECT AppUser.Id, 
       UserName AS Name 
FROM   AppUser 
	     INNER JOIN UserRole 
	     INNER JOIN Role 
WHERE  AppUser.Id=UserRole.UserId 
       AND UserRole.RoleId=Role.Id 
			 AND Role.Deleted = 0 
			 AND Role.Name = 'Business Manager' 
			 AND AppUser.Deleted = 0;